// import { createTranslationManager } from "@cps/static-pages-generator";
// import en from "./.translations/en_US.json";

// const langs = {
// 	en,
// };

// export const getLangSettings = (url) => {
// 	return createTranslationManager(url, langs);
// };

// export const getTranslate = (key, defaultMessage) => {
// 	return key ? key : defaultMessage;
// };
